# Kubernetes Reference

K8s manifests for local testing with Minikube. **Not used in production.**

## Files

- `basic-ingress.yaml` — Example ingress
- `hello-deployment.yaml` — Symlink to app deployment
- `hello-service.yaml` — Symlink to app service
- `managed-cert.yaml` — Example managed certificate (GKE only; unused)

## Local Deployment

For local k8s deployment steps with Minikube, see individual app READMEs (e.g., [apps/hello/README.md](../../apps/hello/README.md)).